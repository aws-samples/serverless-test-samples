import json

# from src import app

def test_lambda_handler_hello_path():
    # ret = app.lambda_handler(apigw_hello_event, lambda_context)
    # expected = json.dumps({"message": "hello unknown!"}, separators=(",", ":"))

    assert True == True

